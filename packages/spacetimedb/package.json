{
  "name": "@battle-circles/spacetimedb",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "spacetime build --module-path .",
    "generate": "spacetime generate --lang typescript --out-dir ../../apps/frontend/src/module_bindings --module-path .",
    "publish:local": "spacetime publish --module-path . --server local",
    "publish": "spacetime publish --module-path . --server maincloud"
  },
  "dependencies": {
    "spacetimedb": "^2.0.2"
  }
}
