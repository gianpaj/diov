/**
 * backend/src/persistence/db.ts
 *
 * Prisma client stub — @prisma/client is not yet installed and no schema
 * exists.  This file satisfies the TypeScript compiler without pulling in the
 * missing package.
 *
 * To enable Prisma:
 *   1. pnpm add @prisma/client          (in backend/)
 *   2. pnpm add -D prisma               (in backend/)
 *   3. npx prisma init                  (creates prisma/schema.prisma)
 *   4. Define your models in schema.prisma
 *   5. npx prisma migrate dev           (runs migrations + generates client)
 *   6. Replace this file with:
 *        import { PrismaClient } from '@prisma/client'
 *        export const prisma = new PrismaClient()
 *
 * See TODO.md — "Persistence (PostgreSQL / Prisma)" for the full task list.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const prisma: any = null
