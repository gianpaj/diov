{
  "name": "@battle-circles/spacetimedb",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "spacetime build --project-path .",
    "generate": "spacetime generate --lang typescript --out-dir ../../src/module_bindings --project-path .",
    "publish:local": "spacetime publish --project-path . --server local",
    "publish": "spacetime publish --project-path . --server maincloud"
  },
  "dependencies": {
    "spacetimedb": "^2.0.2"
  }
}
